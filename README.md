# HTTP-Request-Builder---Local-Offline
A simple, offline HTML tool to convert JSON API request configs into Raw HTTP requests and cURL commands — locally on your machine, with zero data leakage risk.
